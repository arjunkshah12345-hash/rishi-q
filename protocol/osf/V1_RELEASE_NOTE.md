# V1 public freeze snapshot

Immutable public timestamp (do not delete/modify the Release):

https://github.com/arjunkshah12345-hash/rishi-q/releases/tag/prereg-isef2027-v1

The working-tree file `FREEZE_MANIFEST.sha256` is a **living integrity check** for locked
artifacts and may be updated when non-outcome metadata is corrected (e.g. provenance
labels, split preservation). That does **not** rewrite the v1 Release.

V2 candidate materials live under `protocol/isef2027_v2/` and are **not** a published freeze.
