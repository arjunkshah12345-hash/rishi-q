# Pin in Kaggle; keep lean
pandas>=2.2
pyarrow>=16
pyyaml>=6.0
numpy>=1.26
scipy>=1.13
scikit-learn>=1.5
tqdm>=4.66
# Optional GPU stack (install in notebook if Accelerator on):
# torch transformers sentence-transformers accelerate
