name: rishiq-ci

on:
  push:
    branches: [main]
  pull_request:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: astral-sh/setup-uv@v4
        with:
          enable-cache: true
      - name: Install
        run: uv pip install --system -e ".[dev]"
      - name: Unit tests
        run: uv run pytest tests/test_isef2027.py tests/test_core.py -q
      - name: ISEF2027 reproduce harness
        run: uv run rishiq-isef reproduce --config configs/isef2027.yaml
      - name: Confirmatory still locked
        run: |
          out=$(uv run rishiq-isef confirmatory-status)
          echo "$out" | grep -q LOCKED
